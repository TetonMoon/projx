{
  "name": "projx",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "tests/*"
  ],
  "scripts": {
    "build": "tsc -b --pretty",
    "pretest": "npm run build",
    "test": "vitest run",
    "typecheck": "tsc -b --pretty",
    "clean": "tsc -b --clean",
    "cli": "node ./packages/projx-cli/dist/main.js"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
